@using System.Security.Claims;
@if(User.Identities.Any(u => u.IsAuthenticated)) 
{
    
 
  var UserName = User.Identities.First( u => u.IsAuthenticated && u.HasClaim(c => c.Type == ClaimTypes.Name)).FindFirst(ClaimTypes.Name).Value;

  var RoleName = User.Identities.First(u => u.IsAuthenticated && u.HasClaim(c => c.Type == ClaimTypes.Name)).FindFirst(ClaimTypes.Role).Value;


		<a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" href="#"><span class="glyphicon glyphicon-user"></span> 
		@UserName
		</a>
		<ul class="dropdown-menu" role="menu">
      <form id="profile_form"  role="form">
      <div class="form-group" id="profile_content_id">
      <li>
        
          <span class="input-group-addon">
          <img  class="navbar-brand" src="images/mmria-secondary.svg" height=55 width=100 style="padding: 1px 1px;" />
          </span>
      </li>

      <li><strong>Welcome!</strong> 
      <li><strong>User:</strong>@UserName</li>
      <li><strong>ROLES:</strong><ul>
      <li>@RoleName</li>
		
    </li>
    <li>
      <input  value="Log Out" class="btn btn-block  btn-log btn-primary:active:hover1" onclick="profile.logout()"/>
		</li>
		<div><%=version%></div>
    </div>
		</form>
		
		</ul>


} 
else 
{
  <a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" href="#"><span class="glyphicon glyphicon-log-in"></span>  Login</a>
  <ul class="dropdown-menu" role="menu">
@model ApplicationUser
@{    
    <form id="profile_form" asp-antiforgery="true" asp-controller="Account" asp-action="Login" method="POST">
        <li>Nunca
      
      <div class="form-group" id="profile_content_id">
        <div class="input-group">
		    <span class="input-group-addon">
      	<img  class="navbar-brand" src="images/mmria-secondary.svg" height=55 width=100 style="padding: 1px 1px;" />
	      </span>

    
        User name: <input name="username" type="text" />
        Password: <input name="password" type="password" />
        <input name="submit" value="Login" type="submit" />
        <input type="hidden" name="returnUrl" value="@TempData["returnUrl"]" />
   
        <div><%=version%></div>
      </div>
		</li>    
    </form>
} 
  <!--form id="profile_form"  role="form">
    <li>Nunca
      
      <div class="form-group" id="profile_content_id">
        <div class="input-group">
		    <span class="input-group-addon">
      	<img  class="navbar-brand" src="images/mmria-secondary.svg" height=55 width=100 style="padding: 1px 1px;" />
	      </span>
		    <input  id="user_name"  placeholder="Username" type="text" name="email" value="" class="form-control" required />
    		<input id="profile_form_password"  placeholder="Password" type="password" name="password" value="" class="form-control" required />

	      <input  class="btn btn-block  btn-log btn-primary:active:hover1" value="Log in" />
        <div><%=version%></div>
      </div>
		</li>

		<li id="login_status_area"></li>
		
		
		</form-->
		</ul>
}


